{
  "name": "tymsai-api",
  "version": "0.0.1",
  "description": "tymsai-api",
  "main": "index.js",
  "scripts": {
    "dev": "tsc -w -p ./internals/typescript/tsconfig.dev.json",
    "build": "",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^7.0.14"
  },
  "dependencies": {
    "express": "^4.15.2"
  }
}
